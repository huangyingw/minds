some question about engine/containers/php/Dockerfile
    What's $PHPIZE_DEPS? I could not find its definition anywhere.

    If I directly build this file, I would get following error:

        checking for uv_version in -luv... yes
        checking for supported DataStax C/C++ driver version... supported (2.15.1)
        checking for cass_cluster_new in -lcassandra... no
        [91mconfigure: error: Unable to load libcassandra
        [0mThe command '/bin/sh -c apk add --no-cache --virtual build-deps $BUILD_DEPS     && apk add --no-cache libuv gmp     && git clone https://github.com/datastax/php-driver.git $INSTALL_DIR     && cd $INSTALL_DIR     && git submodule update --init     && cd ext && bash $INSTALL_DIR/ext/install.sh     && docker-php-ext-enable cassandra     && apk del build-deps     && rm -rf $INSTALL_DIR' returned a non-zero code: 1

    If I remove $PHPIZE_DEPS from # Cassandra extension, then I could build successfully.
    But, when I use following command:

        rm engine/settings.php
        docker-compose down
        docker-compose build minds_php \
            && docker-compose build installer \
            && docker-compose run installer

    Bellowing is the minds_php, installer section of my docker-compose.yml file:
        minds_php:
            build:
            context: ./engine/containers/php
            image: minds/php:7.3

        installer:
            build:
            context: ./engine
            dockerfile: ./containers/installer/Dockerfile
            networks:
            - app
            volumes:
            - ./front/:/var/www/Minds/front:delegated
            - ./engine/:/var/www/Minds/engine:delegated
            - keys:/.dev
            depends_on:
                - cassandra
                - minds_php


    I would get following error, looks like the Cassandra driver is not installed or configured well in the Dockerfile.        
        - Provisioning Cassandra: [29-Apr-2020 16:33:18 UTC] PHP Fatal error:  Uncaught Error: Class 'Cassandra\RetryPolicy\DowngradingConsistency' not found in /var/www/Minds/engine/Core/Data/Cassandra/Client.php:23
        Stack trace:
        #0 /var/www/Minds/engine/Core/Data/Client.php(35): Minds\Core\Data\Cassandra\Client->__construct(Array)
        #1 /var/www/Minds/engine/Core/Provisioner/Provisioners/CassandraProvisioner.php(30): Minds\Core\Data\Client::build('\\Minds\\Core\\Dat...', Array)
        #2 /var/www/Minds/engine/Core/Provisioner/Installer.php(245): Minds\Core\Provisioner\Provisioners\CassandraProvisioner->provision(false, true)
        #3 /var/www/Minds/engine/Controllers/Cli/Install.php(68): Minds\Core\Provisioner\Installer->provisionCassandra(Object(Minds\Core\Provisioner\Provisioners\CassandraProvisioner), false, true)
        #4 /var/www/Minds/engine/cli.php(60): Minds\Controllers\Cli\Install->exec()
        #5 {main}
        thrown in /var/www/Minds/engine/Core/Data/Cassandra/Client.php on line 23

        Fatal error: Uncaught Error: Class 'Cassandra\RetryPolicy\DowngradingConsistency' not found in /var/www/Minds/engine/Core/Data/Cassandra/Client.php:23
        Stack trace:

Hey, if this is causing an error in the installer, you can call minds install --no-front, then run npm i in the front directory and do the below.
If you want to serve, NODE_OPTIONS=--max_old_space_size=4096 ENGINE_PORT=8080 npm run serve:dev -- --live-reload=false
